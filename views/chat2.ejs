<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>LiveChat</title>
  <link href="/css/style.css" rel="stylesheet">
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="flex flex-col h-screen bg-gray-200">
  <!-- Header -->
  <%- include('partials/header') %>

  <!-- Video Call Block -->
  <div class="fixed videoblock hidden z-[222] w-full h-full bg-black">
    <video class="video-player" id="localVideo" autoplay muted playsinline></video>
    <video class="video-player" id="remoteVideo" autoplay playsinline></video>
  </div>

  <!-- Incoming Call Popup -->
  <div id="incoming-call" class="fixed hidden z-[222] w-full h-full bg-gray-900 bg-opacity-75 flex items-center justify-center">
    <div class="bg-white p-8 rounded-md text-center">
      <p class="mb-4">Incoming Call...</p>
      <button id="accept-call" class="bg-green-500 text-white py-2 px-4 rounded-md">Accept</button>
      <button id="reject-call" class="bg-red-500 text-white py-2 px-4 rounded-md ml-2">Reject</button>
    </div>
  </div>

  <!-- Chat Area -->
  <main id="message-container" class="flex-1 p-4 overflow-y-auto relative">
    <div class="text-zinc-400 absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-2/3 text-center nobody">
      bhagwaan kasam koi nahi hai yaha, ruko shaayad koi aajaye.
    </div>
  </main>

  <!-- Chat Form -->
  <form id="chatform" class="bg-white p-4 flex items-center">
    <input type="hidden" id="userid" value="">
    <input type="text" id="messagebox" class="flex-1 border border-gray-300 rounded-md px-3 py-2 focus:outline-none" placeholder="Type a message">
    <button type="submit" class="ml-2 bg-indigo-600 text-white py-2 px-4 rounded-md hover:bg-indigo-700">Send</button>
    <button type="button" id="next-btn" class="ml-2 bg-gray-500 text-white py-2 px-4 rounded-md hover:bg-gray-600">Next Stranger</button>
    <button type="button" id="screen-share-btn" class="ml-2 bg-green-500 text-white py-2 px-4 rounded-md hover:bg-green-600">Share Screen</button>
  </form>

  <!-- Typing Indicator -->
  <div id="typing-indicator" class="text-sm text-gray-500 mt-1 ml-2"></div>

  <!-- Hidden Videos for WebRTC -->
  <video id="local-video" class="hidden" autoplay></video>
  <video id="remote-video" class="hidden" autoplay></video>

  <!-- Socket.IO -->
  <script src="https://cdn.socket.io/4.7.5/socket.io.min.js" integrity="sha384-2huaZvOR9iDzHqslqwpR87isEmrfxqyWOF7hr7BY6KG0+hVKLoEXMPUJw3ynWuhO" crossorigin="anonymous"></script>
  <script src="/js/chat.js"></script>
</body>

</html>
